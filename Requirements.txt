# Recommended Python packages (pip)

torch
torchvision
scipy
pandas
scikit-learn
vaderSentiment
datasets
sentencepiece
transformers[torch]
accelerate>=0.26.0
safetensors

# Notes:
# - For CUDA wheels, install torch/torchvision with the appropriate index-url from PyTorch (see https://pytorch.org/get-started/locally/).
# - Install with: pip install -r Requirements.txt
pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu129
pip install scipy
pip install pandas
pip install scikit-learn
pip install vaderSentiment
pip install datasets
pip install sentencepiece
pip install transformers[torch]
pip install accelerate>=0.26.0
